import type { Metadata } from "next"
import { WhyNewgenContent } from "./content"

export const metadata: Metadata = {
  title: "Why Join NewGen | Insurance Agent Career | Burlingame, CA",
  description:
    "Build a six-figure career as an independent insurance agent with NewGen. Unlimited income potential, flexible schedule, bilingual market advantage, and a clear leadership path. Serving the Bay Area and beyond.",
}

export default function WhyNewgenPage() {
  return <WhyNewgenContent />
}
